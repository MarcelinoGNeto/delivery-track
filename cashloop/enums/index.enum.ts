export enum UserRole {
  SUPER_ADMIN = "SUPER_ADMIN",
  TENANT_ADMIN = "TENANT_ADMIN",
  OPERATOR = "OPERATOR",
}

export enum PlanType {
  FREE = "FREE",
  PRO = "PRO",
}

export enum CashierStep {
  SEARCH = "SEARCH",
  NOT_FOUND = "NOT_FOUND",
  CREATE = "CREATE",
  OPERATION = "OPERATION",
  SUCCESS = "SUCCESS",
}